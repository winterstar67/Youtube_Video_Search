# YouTube Semantic Search — 최소 의존성 패키지

# YouTube 자막 수집
youtube-transcript-api==1.2.2

# YouTube Data API (메타데이터 수집)
google-api-python-client==2.179.0
google-auth==2.40.3
google-auth-oauthlib==1.2.2
google-auth-httplib2==0.2.0

# 문장 분리 (SaT 모델)
wtpsplit==2.1.6

# Vector DB
pinecone==7.3.0

# 데이터 처리
pandas==2.3.2
numpy==2.3.2

# HTTP 요청
requests==2.32.5

# 설정 파일 파싱
PyYAML==6.0.2

# OpenAI API (선택 — gpt_based_weird_word_sensor.py 사용 시)
openai==1.101.0

# 환경변수 관리
python-dotenv==1.1.1
